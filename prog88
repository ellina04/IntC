#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    if (n > 0) {
        n += 1;
    } else if (n < 0) {
        n -= 2;
    } else {
        n = 10;
    }
    cout << n << endl;
    return 0;
}
