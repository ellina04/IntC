#include <math.h> 
#include <iostream>

using namespace std;

int main()
{
    float a, b, x;
    cout << "Введите число:" << endl;
    cin >> a;
    cout << "Введите число:" << endl;
    cin >> b;
    if(a==0 && b==0){
        cout << "Нельзя" << endl;
    }
    else 
        x = pow(a,2)*pow(b,2);
    cout << "Ответ" << x << endl;
    return 0;
}
