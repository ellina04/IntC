#include <math.h> 
#include <iostream>

using namespace std;

int main()
{
    float L, R, S, p;
    cout << "Введите число:" << endl;
    cin >> R;
    p = 3.14;
    L = 2*p*R;
    S = p* pow(R,2);
    cout << "Ответ " << S << ", " <<  L << endl;
    return 0;
}
