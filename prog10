#include <math.h> 
#include <iostream>

using namespace std;

int main()
{
    float a, b, x;
    cout << "Введите число:" << endl;
    cin >> a;
    cout << "Введите число:" << endl;
    cin >> b;
    x = (a+b)/2;
    cout << "Ответ: " << x << endl;

    return 0;
}
