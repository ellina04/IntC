#include <math.h> 
#include <iostream>

using namespace std;

int main()
{
    float x, y;
    cout << "Введите число:" << endl;
    cin >> x;
    y= 3*pow(x,6)-6*pow(x,2)-7;
    cout << "Ответ " << y  << endl;
    return 0;
}
